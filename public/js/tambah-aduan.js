// Tambah Aduan - Form validation (optional)
document.addEventListener('DOMContentLoaded', function() {
  // Form validation bisa ditambahkan di sini jika diperlukan
  const form = document.getElementById('formAduan');
  if (form) {
    form.addEventListener('submit', function(e) {
      // Validasi bisa ditambahkan di sini
      // Jika validasi gagal, gunakan e.preventDefault()
    });
  }
});

